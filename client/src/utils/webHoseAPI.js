
import APIKEY from "./keys";

const BASEURL = 'https://webhose.io/search';
const query = "&q=iphone";

const options = {
	format: 'json',
	language: 'english',
	site_type: 'news',
	size: 10
};

// module.exports = {
// 	BASEURL, APIKEY,  query, options
// };
